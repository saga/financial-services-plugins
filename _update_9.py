#!/usr/bin/env python3
# Update clean-data-xls/SKILL.md
file_path = '/Users/saga/code-repos/financial-services-plugins/中文版/plugins/vertical-plugins/financial-analysis/skills/clean-data-xls/SKILL.md'

with open(file_path, 'r', encoding='utf-8') as f:
    content = f.read()

marker = '> **💡 Appendix: 领域知识小贴士'
idx = content.find(marker)
if idx < 0:
    print('ERROR: marker not found')
    exit(1)

before = content[:idx].rstrip('\n') + '\n\n---\n\n'

appendix = '''## Appendix: 金融背景知识

这份文件是"数据清洗（Clean Data XLS）"技能的详细说明——把混乱的 Excel 数据标准化、去重、修复格式，让金融模型能"消化"它。读完这个附录，你会搞懂：**金融数据为什么是"垃圾进，垃圾出"（GIGO）的源头、脏数据在金融世界里能造成什么样的灾难、财务模型为什么对"文本数字"零容忍、日期的跨国"翻译"为什么可以产生诉讼级错误、以及数据清洗为什么是金融分析的"地基工程"**。

> **通用金融知识已抽离到独立知识库，按需阅读：**
>
> - [财务会计](../../../../../金融知识学习/财务会计.md) — 三表结构、会计科目
> - [估值方法](../../../../../金融知识学习/估值方法.md) — 财务模型对数据的要求
>
> 下面只讲**本文件独有**的内容。

---

### 1. GIGO——"垃圾进，垃圾出"

**GIGO（Garbage In, Garbage Out）** 是金融模型的第一定律：如果你把垃圾数据喂给一个金融模型，即使模型本身完美无缺，输出的结果也是垃圾。

**打比方——做蛋糕**：
- 你有一个世界顶级的烤箱（= 完美的金融模型）
- 但你把发霉的面粉、过期的鸡蛋、工业酒精倒进去（= 脏数据）
- 再好的烤箱也烤不出能吃的蛋糕（= 输出结果毫无价值）

**金融模型的 GIGO 经典案例**：

| 脏数据类型 | 如果不清洗 | 造成的后果 |
|-----------|----------|-----------|
| 文本存储的数字 | SUM 函数视其为 0 | 营收总和少了几十亿 |
| 日期格式混乱 | 3/8/26 和 8/3/26 分不清 | 把 Q1 数据当成 Q3 用了 |
| 大小写不一致 | "USA" vs "usa" | 分类统计多了一个"重复国家" |
| 前后空格 | " Apple " vs "Apple" | 公司名称匹配失败，漏算关键数据 |
| 重复行 | 同一笔交易录了两次 | 营收/成本都被高估 |

**为什么在金融中 GIGO 特别致命**：
- 金融模型的输出是"真金白银"的投资决策
- 一个数据清洗错误 → 估值偏差 ±$100M → 投资决策错误
- 而投资决策错误可能是永久的——亏了就是亏了

---

### 2. "文本数字"——金融模型的"隐形杀手"

这是 Excel 中使用金融数据时最常见的坑：

**什么是"文本数字"**：
在 Excel 中，一个数字如果被存储为"文本"格式（单元格左上角有个绿色小三角），所有 SUM/AVERAGE/MAX/MIN 等函数都会**把它当成 0 来处理**。

**为什么会变成文本数字**：
- 从 PDF 或网页复制数据到 Excel 时，格式丢失
- 从数据库导出时，系统把数字存成了文本
- 用户手动输入时在前面或后面多打了一个空格
- 数字包含特殊字符——如 "$1,000"（Excel 认为这是文本，因为有 $ 和 ,）

**真实世界中的灾难**：
- 某 PE 基金的实习生从公司年报 PDF 中复制了资产负债表数据
- 其中"现金及现金等价物 = $500,000,000"被存成了文本
- 基金在做 LBO 模型时，现金这一行的 SUM = 0
- 结果：模型显示目标公司"现金为 0"→ 需要借更多钱 → IRR 被低估了 8%
- 基金差点因此放弃了这笔交易——直到一位 VP 发现了这个"隐形杀手"

**预防方法**：
- 永远检查你的数据列是否有"小绿三角"
- 使用 =VALUE() 或 =NUMBERVALUE() 函数转换
- 在做任何分析之前，先检查 SUM 的结果是否合理

---

### 3. 日期的"跨国翻译"——3 月 8 日还是 8 月 3 日？

这是处理跨国金融数据时最"烧脑"的问题：

**日期格式差异**：
| 国家/地区 | 格式 | 例子 | 
|----------|------|------|
| 美国 | MM/DD/YYYY | 3/8/2026 = 3 月 8 日 |
| 英国/欧洲 | DD/MM/YYYY | 3/8/2026 = 8 月 3 日 |
| 中国/日本 | YYYY/MM/DD | 2026/3/8 = 3 月 8 日 |
| ISO 标准 | YYYY-MM-DD | 2026-03-08 = 3 月 8 日 |

**在金融中的"日期撞车"**：
- 你从一家英国银行收到的数据是 DD/MM/YYYY
- 你从一家美国投行收到的数据是 MM/DD/YYYY
- 你把两个数据合并到一个 Excel 中
- 3/8/2026 在其中一个文件中是 3 月 8 日，在另一个是 8 月 3 日
- → 你把 Q1 的营收和 Q3 的成本对接了 → 毛利率完全算错

**为什么在金融中这特别危险**：
- 利息计算依赖于时间跨度：如果起止日期搞错了，利息就算错了
- 季度财报排序：如果把日期搞反了，Q1 和 Q3 的数据就颠倒了
- 审计追踪（Audit Trail）：如果日期不一致，审计师会质疑你的数据完整性

**"永远标准化日期"**：
在处理任何金融数据前，第一件事就是把日期统一为 ISO 标准（YYYY-MM-DD）——这是唯一不会产生歧义的格式。

---

### 4. 审计追踪——"把你的推导步骤写清楚"

**审计追踪（Audit Trail）** 是金融合规的核心概念。

**什么是审计追踪**：
如果别人想知道你的数据哪里来的、经过什么处理、为什么最终是这个数字——他们应该能"沿着一条清晰的链"追溯到源头。

**打比方——破案**：
侦探侦破一个案子，需要能从"嫌疑人被捕"一步一步倒推回"第一个线索"。金融数据也一样——从最终估值倒推回原始数据源。

**为什么数据清洗需要审计追踪**：
- 如果你直接把原始数据"原位覆盖"（原地修改），别人就无法知道原始数据长什么样了
- 如果你用"辅助列"写一个公式：`=TRIM(A2)` 或 `=VALUE(B2)`，别人可以看到你的清洗逻辑
- **公式 > 硬编码**：公式是透明的，硬编码是黑箱

**在真实投行场景**：
- EDGAR（美国 SEC 数据库）上下载的财报数据 → 原始数据必须保留（不可修改）
- 在旁边的"辅助列"中写清洗公式 → 这一步是透明的
- 清洗后的数据用于模型 → 如果有人质疑，可以追溯回原始数据

**这是华尔街的铁律**——你不能"改了别人的数据还不留痕迹"。

---

### 5. 金融数据清洗的"脏数据排行榜"

从最常遇到 → 最灾难性：

| 排名 | 脏数据类型 | 频率 | 危害 |
|------|----------|------|------|
| 🥇 1 | **文本数字** | 极高 | 高——SUM 直接忽略 |
| 🥈 2 | **前后空格** | 极高 | 中——分类/匹配失败 |
| 🥉 3 | **日期格式混乱** | 高 | 极高——时序全乱（诉讼级别） |
| 4 | **重复行** | 中 | 高——数字翻倍 |
| 5 | **大小写不一致** | 中 | 中——分类错乱 |
| 6 | **特殊字符（$, %, ()）** | 中 | 中——VALUE 转换失败 |
| 7 | **编码乱码（Ã©, â€™）** | 低 | 高——数据不可读 |
| 8 | **空值/Null** | 中 | 中——公式报错 |

**投行数据清洗的真实流程**：
1. 从数据源导出（Bloomberg、FactSet、EDGAR）
2. 单独保存一份"原始数据"（永不修改）
3. 创建"清洗版本"——用公式而非原位覆盖
4. 抽样检查（随机抽查 10% 的清洗结果是否正确）
5. 将清洗后的数据导入财务模型

---

### 给小白的一句话

> **数据清洗是金融分析的"地基工程"——GIGO（垃圾进，垃圾出）是金融模型的第一定律。"文本数字"是最常见的隐形杀手——Excel 会把它们当 0 处理，让你的 SUM 函数失效。跨国日期格式（美国 MM/DD vs 欧洲 DD/MM）可以产生"诉讼级错误"——把 Q1 数据当 Q3 用，利息计算完全错误。审计追踪（Audit Trail）是金融合规的铁律——永远用"辅助列 + 公式"而不是"原位覆盖"，让你的数据来源和处理逻辑可追溯。清洗前先保存一份原始数据，清洗后随机抽样核查——宁可多花 30 分钟清洗，不要因为一个数字错误导致 $100M 的估值偏差。**
'''

with open(file_path, 'w', encoding='utf-8') as f:
    f.write(before + appendix)

print(f'SUCCESS: clean-data-xls/SKILL.md updated ({len(appendix)} chars)')